// API Configuration
// ⚠️  IMPORTANT: Update API_BASE_URL before running the app!

export const API_CONFIG = {
  // Change this to your actual backend server URL
  // Android Emulator (local): 'http://10.0.2.2:5000/api'
  // Physical Device (local): 'http://192.168.x.x:5000/api' (replace x.x with your computer IP)
  // Production: 'https://your-domain.com/api'
  API_BASE_URL: 'https://sankha.online/api',  // ← Changed to HTTPS
};

export default API_CONFIG;
